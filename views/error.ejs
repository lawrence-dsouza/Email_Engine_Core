<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - <%= statusCode %></title> <!-- Display error status code in title -->
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f7fc;
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .error-container {
      text-align: center;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      width: 350px;
    }

    h1 {
      font-size: 36px;
      color: #ff4d4f; /* Red color for the error */
    }

    p {
      font-size: 18px;
      color: #555;
    }

    .error-btn {
      margin-top: 20px;
      padding: 12px 24px;
      background-color: #0078d4;
      color: white;
      font-size: 16px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .error-btn:hover {
      background-color: #005a9e;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <h1><%= statusCode %> <%= statusMessage %></h1> <!-- Dynamic error code and message -->
    <p><%= errorMessage %></p> <!-- Display dynamic error message -->
    <a href="/">
      <button class="error-btn">Go to Login</button>
    </a>
  </div>
</body>
</html>
